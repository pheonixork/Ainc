/* eslint-disable react/no-unescaped-entities */
import React from 'react';
import Box from '@mui/material/Box';
import PropTypes from 'prop-types';

const ReportPage = ({...rest  }) => {
  return (
    <Box {...rest}>
      <p>Report</p>
    </Box>
  );
};

export default ReportPage;
